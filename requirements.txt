# UPVN — headless test + lint
# No runtime deps for headless (UPBGE provides bpy/bge/aud/blf at runtime).
# The engine renders via UPBGE (bge/blf) — there is NO Pillow renderer in the engine.

# testing
pytest>=7
fake-bge-module>=2023.0; python_version < "3.13"

# dev / lint (not required to run game)
black
# Pillow: only for standalone dev tools (tools/generate_screenshots*.py,
# tools/make_placeholder_art.py). NOT used by the engine or the test suite.
pillow>=10
